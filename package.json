{
  "name": "dsh-desktop",
  "private": true,
  "version": "0.4.5",
  "description": "DSH Desktop — 内嵌 DSH Web GUI 的 Windows 桌面客户端（Tauri 2）",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.5.0",
    "@tauri-apps/plugin-autostart": "^2.2.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.5.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
