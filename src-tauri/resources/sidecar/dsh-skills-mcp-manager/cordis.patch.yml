# dsh-skills-mcp-manager bundle patch: inserts the dual-face plugin row into the
# web profile roster. Applied as a profile bundle layer (the `dsh.bundle.patch`
# manifest field).
#
# The node half (exports ".") runs in the host process (skills filesystem engine
# + MCP connections + /api/skills-mcp routes); the `dsh.client` declaration in
# package.json makes the browser half (exports "./client", served at
# /plugins/<id>/client.js) mount a theme-aware settings section.
#
# `id` is stable so the user's own ~/.dsh/cordis.patch.yml can toggle this row;
# `name` is the published package name and must match package.json.
- insert:
    - id: ui-dsh-skills-mcp-manager
      name: '@dsh-desktop/dsh-skills-mcp-manager'
